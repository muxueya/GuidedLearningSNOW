// NOTE: This project uses Tailwind CSS v4.
// In v4, this JavaScript config file is NOT read — configuration lives in globals.css via @theme directives.
// Custom animations (e.g. fade-in) must be added to app/globals.css using:
//   @theme { --animate-fade-in: fade-in 0.3s ease-out; }
//   @keyframes fade-in { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }
// This file is kept only to satisfy editors that expect it.
export default {}
